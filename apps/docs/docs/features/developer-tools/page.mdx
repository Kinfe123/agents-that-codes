---
title: "Developer Tools"
description: "The project includes developer-facing tools, generators, inspectors, or visual editors."
---

# Developer Tools

The project includes developer-facing tools, generators, inspectors, or visual editors.
## What This Is
This section explains the utilities that help developers inspect, configure, or generate parts of the product. These tools are not just UI screens; they are shortcuts for common setup, debugging, schema, email, or integration work.

Repository context: You can deploy your own version of the coding agent template to Vercel with one click:
## Why It Matters
Developer Tools matters because it is part of how Coding Agent Template turns repository behavior into a usable developer or operator workflow. This page gives readers the mental model first, then uses implementation evidence as supporting context.
## Core Concepts
- developer tools
- generators
- inspectors
- visual editors
- debug workflows
## Typical Workflow
1. Choose the tool that matches the setup or debugging task.
2. Review generated code or detected state before applying it to the host project.
3. Use tool output as a starting point, then validate it against the application's real configuration.
## Detailed Behavior
Developer Tools works as a product workflow with three layers. The first layer is the user-facing experience: what an operator sees, what they can search or inspect, and what actions they can take. The second layer is the domain model: which records, relationships, or configuration values the workflow depends on. The third layer is the safety model: which permissions, access rules, or validation steps prevent accidental or unauthorized changes.

For Coding Agent Template, this feature is easiest to understand from the operator's point of view. A reader can understand what problem this page solves, what state it reads or changes, and what a successful outcome looks like before they need to think about source files.

## Common Scenarios
- A new operator wants to understand what developer tools is for before using it in production.
- A developer is wiring the feature into a host application and needs to know which data, config, or permissions are involved.
- A maintainer changed the feature and needs to update docs without forcing readers to understand the source tree first.

## Success Criteria
- Readers can explain what developer tools does in plain language.
- Readers know the main objects or settings involved before they see any file paths.
- Readers know which actions are safe, which actions are destructive, and which actions depend on access control.
## How It Fits in the Product
Developer Tools is a product capability, not just a list of source files. The important story is the user-facing outcome, the data or configuration it depends on, and the guardrails that make the workflow safe to operate.
## Repository Notes
These notes come from the repository README so the page stays grounded in existing project language.

### Deploy Your Own
You can deploy your own version of the coding agent template to Vercel with one click:

[](https://vercel.com/new/clone?repository-url=https%3A%2F%2Fgithub.com%2Fvercel-labs%2Fcoding-agent-template&env=POSTGRES_URL,ANTHROPIC_API_KEY,GITHUB_TOKEN,VERCEL_TEAM_ID,VERCEL_PROJECT_ID,VERCEL_TOKEN,AI_GATEWAY_API_KEY,CURSOR_API_KEY,NPM_TOKEN&envDescription=Environment+variables+for+the+coding+agent+template.+AI_GATEWAY_API_KEY+is+required+for+branch+name+generation+and+Codex+agent.+CURSOR_API_KEY+and+NPM_TOKEN+are+optional.&project-name=coding-agent-template&repository-name=coding-agent-template)

### Features
- **Multi-Agent Support**: Choose from Claude Code, OpenAI Codex CLI, Cursor CLI, or opencode to execute coding tasks
- **Vercel Sandbox**: Runs code in isolated, secure sandboxes ([docs](https://vercel.com/docs/vercel-sandbox))
- **AI Gateway Integration**: Built for seamless integration with [Vercel AI Gateway](https://vercel.com/docs/ai-gateway) for model routing and observability
- **AI-Generated Branch Names**: Automatically generates descriptive Git branch names using AI SDK 5 + AI Gateway
- **Task Management**: Track task progress with real-time updates
- **Persistent Storage**: Tasks stored in Neon Postgres database
- **Git Integration**: Automatically creates branches and commits changes
- **Modern UI**: Clean, responsive interface built with Next.js and Tailwind CSS

### How It Works
1. **Task Creation**: When you submit a task, it's stored in the database
2. **AI Branch Name Generation**: AI SDK 5 + AI Gateway automatically generates a descriptive branch name based on your task (non-blocking using Next.js 15's `after()`)
3. **Sandbox Setup**: A Vercel sandbox is created with your repository
4. **Agent Execution**: Your chosen coding agent (Claude Code, Codex CLI, Cursor CLI, or opencode) analyzes your prompt and makes changes
5. **Git Operations**: Changes are committed and pushed to the AI-generated branch
6. **Cleanup**: The sandbox is shut down to free resources
## Operational Notes
- The concept comes before the file paths, so readers understand the workflow before they inspect implementation evidence.
- Examples stay grounded in detected commands, config names, import paths, or behavior from the repository.
- Maintainer details live in the reference section so the main page stays useful to product readers.
- User-facing state, available actions, empty/error states, and permission boundaries are part of the product behavior.
- The page explains what data the feature reads or changes before referencing source files.
## Maintainer Reference
The sections above are the reader-facing explanation. The references below are for maintainers who need to verify or update the implementation behind this page.

### Implementation Map
- `components/task-page-client.tsx` - Evidence file that contributes to the implementation understanding for this page.
- `lib/db/client.ts` - Evidence file that contributes to the implementation understanding for this page.
- `lib/sandbox/agents/claude.ts` - Evidence file that contributes to the implementation understanding for this page.
- `lib/sandbox/commands.ts` - Evidence file that contributes to the implementation understanding for this page.

### Source Details
- `components/task-page-client.tsx`: language TypeScript; exports `TaskPageClient`
- `lib/db/client.ts`: language TypeScript; exports `db`; env `POSTGRES_URL`
- `lib/sandbox/agents/claude.ts`: language TypeScript; exports `executeClaudeInSandbox`, `installClaudeCLI`; env `ANTHROPIC_API_KEY`; flags `--dangerously-skip-permissions`, `--help`, `--porcelain`, `--version`
- `lib/sandbox/commands.ts`: language TypeScript; exports `CommandResult`, `runCommandInSandbox`, `runStreamingCommandInSandbox`, `StreamingCommandOptions`

### Evidence
- `components/task-page-client.tsx`
- `lib/db/client.ts`
- `lib/sandbox/agents/claude.ts`
- `lib/sandbox/commands.ts`
- `README.md#deploy-your-own`
- `README.md#features`
- `README.md#how-it-works`
- `README.md#tech-stack`
