---
title: "Database Adapters"
description: "The project detects or configures database adapters."
---

# Database Adapters

The project detects or configures database adapters.
## What This Is
Database adapter docs explain how the product connects to the same persistence layer as the host auth system. They identify supported adapters, the expected provider names, and what needs to be migrated before the UI can read or write records safely.
## Why It Matters
Database Adapters matters because it is part of how Coding Agent Template turns repository behavior into a usable developer or operator workflow. This page gives readers the mental model first, then uses implementation evidence as supporting context.
## Core Concepts
- Prisma
- Drizzle
- SQLite
- Postgres
- MySQL
- schema detection
## Typical Workflow
1. Identify which adapter the host Better Auth project already uses.
2. Confirm that the product can load the adapter and database connection from the host configuration.
3. Run migrations or schema pushes before using features that require additional columns.
## Detailed Configuration Model
Database Adapters controls behavior rather than UI copy. The important questions are what decision this configuration helps a developer make, what happens when it is absent, and what runtime behavior changes when it is enabled. This page keeps the mental model ahead of the raw files that mention the setting.

For Coding Agent Template, configuration usually answers one of four questions: who can access the product, where it is mounted, how it talks to auth or database state, and what optional capabilities are enabled. Each setting is documented in relation to one of those questions.

## Decision Points
- Is the setting required for every installation, or only for a specific framework, adapter, or deployment mode?
- Does the setting affect security, persistence, presentation, event tracking, or developer convenience?
- What does a developer verify after changing it?

## Success Criteria
- Readers know when to configure this area and when they can ignore it.
- Readers understand the consequence of a wrong value.
- Readers can connect the setting to a visible product behavior or runtime integration.
## Configuration Surface
Configuration is easiest to understand when each setting is tied to the behavior it controls. The tables below collect the relevant files and environment variables after the mental model, so readers can connect the concept to the concrete setup surface.
### Files
- `drizzle.config.ts` (database) - Database schema or adapter configuration.
- `lib/db/migrations/meta/_journal.json` (database) - Database schema or adapter configuration.
- `lib/db/migrations/meta/0000_snapshot.json` (database) - Database schema or adapter configuration.
- `lib/db/migrations/meta/0001_snapshot.json` (database) - Database schema or adapter configuration.
- `lib/db/migrations/meta/0002_snapshot.json` (database) - Database schema or adapter configuration.
- `lib/db/migrations/meta/0003_snapshot.json` (database) - Database schema or adapter configuration.
- `lib/db/migrations/meta/0004_snapshot.json` (database) - Database schema or adapter configuration.
- `lib/db/migrations/meta/0005_snapshot.json` (database) - Database schema or adapter configuration.
### Environment Variables
- `AI_GATEWAY_API_KEY`
- `ANTHROPIC_API_KEY` (default: `sk-ant-api03-your-key-here`) - Required API Keys
- `CURSOR_API_KEY` (default: `your-cursor-api-key`) - Optional API Keys (depending on which agents you want to use)
- `ENCRYPTION_KEY` (default: `your-32-byte-hex-key`) - Optional Encryption Key (used to encrypt MCP secrets in the database)
- `GITHUB_TOKEN` (default: `ghp_your-github-token-here`)
- `NPM_TOKEN` (default: `npm_your-npm-token`)
- `OPENAI_API_KEY` (default: `sk-your-openai-key-here`)
- `POSTGRES_URL` (default: `postgresql://username:password@localhost:5432/coding_agent`) - Database
- `VERCEL_PROJECT_ID` (default: `your-project-id`)
- `VERCEL_TEAM_ID` (default: `your-team-id`) - Vercel Sandbox Configuration
- `VERCEL_TOKEN` (default: `your-vercel-token`)
## Repository Notes
These notes come from the repository README so the page stays grounded in existing project language.

### 4. Set up the database
Generate and run database migrations:

```bash
pnpm db:generate
pnpm db:push
```

### Database Operations
```bash
# Generate migrations
pnpm db:generate

# Push schema changes
pnpm db:push

# Open Drizzle Studio
pnpm db:studio
```
## How to Reason About This Config
- Identify whether the option affects security, runtime mounting, data access, or presentation.
- Prefer examples that use the same names and defaults found in the repository evidence.
- Call out adapter-specific behavior instead of implying every setting applies to every framework.
## Validation Checklist
- Keep config examples aligned with source defaults and runtime validation.
- Mention whether a setting is required, optional, or only needed for a specific adapter.
- Update this page when config object names, env vars, or supported providers change.
## Operational Notes
- The concept comes before the file paths, so readers understand the workflow before they inspect implementation evidence.
- Examples stay grounded in detected commands, config names, import paths, or behavior from the repository.
- Maintainer details live in the reference section so the main page stays useful to product readers.
- Required settings, optional settings, defaults, and adapter-specific behavior are separated clearly.
- Security impact and validation expectations are called out before examples.
## Maintainer Reference
The sections above are the reader-facing explanation. The references below are for maintainers who need to verify or update the implementation behind this page.

### Implementation Map
- `drizzle.config.ts` - Evidence file that contributes to the implementation understanding for this page.
- `lib/db/migrations/meta/_journal.json` - Evidence file that contributes to the implementation understanding for this page.
- `lib/db/migrations/meta/0000_snapshot.json` - Evidence file that contributes to the implementation understanding for this page.
- `lib/db/migrations/meta/0001_snapshot.json` - Evidence file that contributes to the implementation understanding for this page.
- `lib/db/migrations/meta/0002_snapshot.json` - Evidence file that contributes to the implementation understanding for this page.
- `lib/db/migrations/meta/0003_snapshot.json` - Evidence file that contributes to the implementation understanding for this page.

### Source Details
No source-level declarations, routes, environment variables, or CLI flags were detected for these evidence files.

### Evidence
- `drizzle.config.ts`
- `lib/db/migrations/meta/_journal.json`
- `lib/db/migrations/meta/0000_snapshot.json`
- `lib/db/migrations/meta/0001_snapshot.json`
- `lib/db/migrations/meta/0002_snapshot.json`
- `lib/db/migrations/meta/0003_snapshot.json`
- `package.json:@neondatabase/serverless`
- `package.json:drizzle-kit`
- `package.json:drizzle-orm`
- `package.json:postgres`
- `README.md#3-set-up-environment-variables`
- `README.md#4-set-up-the-database`
